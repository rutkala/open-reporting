# dbr static dashboard build artifacts — regenerated by `dbr run` / run_daily from
# YAML + warehouse data (see packages/dbr static_export). Not source; not committed.
# The vendored plotly.min.js lives under assets/. Tracked portal files (index.html,
# *.png) sit at this top level and are unaffected by these subdirectory ignores.
*/
