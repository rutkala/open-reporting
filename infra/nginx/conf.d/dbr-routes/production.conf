# dbr-managed — static dashboard, no backend. Re-written by `dbr run`. Manual edits lost.
location = /production { return 301 /production/; }
location /production/ {
    try_files $uri $uri/ /production/index.html;
    add_header Cache-Control "public, max-age=3600" always;
}
