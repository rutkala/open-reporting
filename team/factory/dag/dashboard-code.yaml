name: Dashboard Code
description: Dashboard Code - assembled dashboard application

# Component producer
workstation: full-stack-development

# Internal steps to produce this component
steps:
  - environment_setup
  - data_layer
  - measure_integration
  - layout_implementation
  - component_wiring
  - local_verification
  - finalize

# Step dependencies
dependencies:
  environment_setup: []
  data_layer: [environment_setup]
  measure_integration: [data_layer]
  layout_implementation: [measure_integration]
  component_wiring: [layout_implementation]
  local_verification: [component_wiring]
  finalize: [local_verification]

# Output format
produces:
  - Dashboard Code

# Input requirements
input:
  - UX/UI Design
  - Semantic Model