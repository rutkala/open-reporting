name: Release Document
description: Release Document - deployment details and checklist

# Component producer
workstation: devops

# Internal steps to produce this component
steps:
  - code_cleanup
  - pr_creation
  - evidence_attachment
  - review_process
  - merge_deploy
  - finalize

# Step dependencies
dependencies:
  code_cleanup: []
  pr_creation: [code_cleanup]
  evidence_attachment: [pr_creation]
  review_process: [evidence_attachment]
  merge_deploy: [review_process]
  finalize: [merge_deploy]

# Output format
produces:
  - Release Document

# Input requirements
input:
  - QA Report