# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/visual.schema.json
# Pobór wody — najnowsza wartość z deltą r/r.
# ascending_is_good=false: redukcja poboru wody jest pożądana.

type: card
encoding:
  value: { metric: water_abstractions }
filter:
  geo: PL
options:
  delta:
    vs: prior_year
    format: icon-first
