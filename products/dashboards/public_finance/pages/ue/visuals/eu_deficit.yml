# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/visual.schema.json
# Deficyt fiskalny UE-27, dwa najnowsze lata side-by-side — Polska wyróżniona.

type: bar
encoding:
  x: { metric: fiscal_balance }
  y: { dimension: geo__country_name_pl }
filter:
  geo: [AT, BE, BG, CY, CZ, DE, DK, EE, EL, ES, FI, FR, HR, HU, IE, IT, LT, LU, LV, MT, NL, PL, PT, RO, SE, SI, SK]
options:
  dual_year: true
  sort: value-asc
  highlight:
    value: Polska
    color: azure_1
    other: slate_3
  reference_lines:
    - { value: -3, label: "Próg SGP", color: negative }
  download: true
