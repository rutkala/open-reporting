# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/visual.schema.json
# Dług publiczny Polski — najnowsza wartość roczna.

type: card
encoding:
  value: { metric: public_debt }
filter:
  geo: PL
options:
  threshold:
    rule: maastricht_debt
