# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/visual.schema.json
# Oczekiwana długość życia kobiet — najnowsza wartość z deltą r/r.

type: card
encoding:
  value: { metric: life_expectancy_f }
filter:
  geo: PL
options:
  delta:
    vs: prior_year
    format: icon-first
