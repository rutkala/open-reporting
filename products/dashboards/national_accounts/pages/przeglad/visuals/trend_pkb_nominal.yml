# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/visual.schema.json
# PKB nominalny w mld EUR — historia 2004–2024.
# Line chosen: continuous upward level/stock concept — trajectory matters more than discrete steps.

type: line
encoding:
  x: { dimension: date_key__cal_year }
  y: { metric: gdp_nominal }
filter:
  geo: PL
