# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/page.schema.json
# Page-level config for the overview page.
# Only `title` and `anchor` are required.

title:  Przegląd
anchor: overview
