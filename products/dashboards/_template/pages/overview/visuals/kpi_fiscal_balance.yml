# yaml-language-server: $schema=https://open-reporting.dev/schemas/visual.schema.json
# One KPI card bound to the fiscal_balance metric for Poland.
# `type` and `metric` are required; everything else inherits defaults.

type:   kpi_standard
metric: fiscal_balance
filter:
  geo: PL
