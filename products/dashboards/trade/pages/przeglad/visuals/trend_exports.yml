# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/visual.schema.json
# Eksport towarów — historia roczna, Polska, 2002–2025.
# Line chosen: continuous annual mln EUR; the 8× growth from 43B to 366B EUR
# tells Poland's integration into EU supply chains and export-led growth.

type: line
encoding:
  x: { dimension: date_key__cal_year }
  y: { metric: goods_exports }
filter:
  geo: PL
