# yaml-language-server: $schema=https://open-reporting.dev/dbr/schemas/visual.schema.json
# Import towarów (mln EUR) — bieżąca wartość z deltą r/r.

type: card
encoding:
  value: { metric: goods_imports }
filter:
  geo: PL
options:
  delta:
    vs: prior_year
    format: icon-first
