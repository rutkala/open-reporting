version: 2

sources:
  - name: raw
    schema: raw
    tables:
      - name: eurostat_observations
        description: >
          Raw Eurostat SDMX observations. One row per
          dataset × geo × period × dimension combination. Ingested via
          products/ingestion/to_raw/eurostat_observations.py.
      - name: imf_weo
        description: >
          Raw IMF World Economic Outlook fiscal indicators. One row per
          weo_subject × iso_code × year × weo_edition. Ingested via
          products/ingestion/to_raw/imf_weo.py.
