version: 2

models:
  - name: metricflow_time_spine
    description: Day-grain calendar required by MetricFlow as the canonical time spine.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
