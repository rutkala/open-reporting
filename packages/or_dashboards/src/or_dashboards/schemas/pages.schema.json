{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://open-reporting.dev/schemas/pages.schema.json",
  "title": "or-dashboards pages/pages.yml",
  "description": "Lists the dashboard's pages in the order they appear in the sidebar.",
  "type": "object",
  "required": ["order"],
  "additionalProperties": false,
  "properties": {
    "order": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_-]*$",
        "description": "Page name — must match a sibling folder containing `page.yml`."
      }
    }
  }
}
