"""Dashboard skill assets — building blocks for Open Reporting dashboards.

One folder per block. Each block is self-contained (code + future docs/tests
colocated). Dashboards import a block by name:

    from or_dashboards.make_app import make_app

The old layout lives at `assets_legacy/` for reference.
"""
