# TODO_DOMAIN dashboard — runtime dependencies.
#
# Copy to ``products/dashboards/TODO_DOMAIN/requirements.txt`` and pin
# the versions you actually use. Production hosts install only this
# file; ``requirements-dev.txt`` brings in test/lint extras and is
# never installed in production.
#
# Deliberately not included: gunicorn, uvicorn. Open Reporting runs
# dashboards directly via systemd ``python3 app.py`` — see
# ``specs/deploy/deploy.md``.

dash>=4.1
pandas>=2.0
duckdb>=1.0
python-dotenv>=1.0
