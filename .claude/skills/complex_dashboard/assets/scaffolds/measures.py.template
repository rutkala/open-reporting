"""
TODO_DOMAIN dashboard — dimension and measure display config.

Copy this file to ``products/dashboards/TODO_DOMAIN/measures.py`` and
replace the entries with the dimensions and measures the domain needs.

This file carries **display metadata only** — labels, units, format
settings. All aggregation logic lives upstream in
``data_loaders.py``; this file never queries the warehouse.

Column-naming convention (matches ``data_loaders_template.py``):

- ``Dimension(name, label, column)``  — column is ``dim_<name>``
- ``Measure(name, label, column, …)`` — column is ``val_<name>``

The report consumes ``DIMS["x"]`` and ``MEASURES["y"]`` through the
interface in ``specs/data/data_loaders.md`` — never instantiates
``Measure`` or ``Dimension`` itself.

For breadth (15+ measures, geo / OHLC / derived measures) see
``products/dashboards/template/measures.py``.
"""
from complex_dashboard.assets.semantic import Dimension, Measure


# ── Dimensions ───────────────────────────────────────────────────────────────

DIMS = {
    "year":   Dimension("year",   "Rok",       "dim_year"),
    "region": Dimension("region", "Województwo", "dim_region"),
}


# ── Measures ─────────────────────────────────────────────────────────────────

MEASURES = {
    "unemployment": Measure(
        "unemployment", "Stopa bezrobocia", "val_unemployment",
        unit="%", format_type="percent", decimals=1,
    ),
    "employment": Measure(
        "employment", "Zatrudnienie", "val_employment",
        unit="tys. osób", format_type="number", scale="K", decimals=0,
    ),
    "wages": Measure(
        "wages", "Przeciętne wynagrodzenie", "val_wages",
        format_type="currency", currency_symbol="zł", decimals=0,
    ),
    "wage_growth": Measure(
        "wage_growth", "Dynamika wynagrodzeń (r/r)", "val_wage_growth",
        format_type="percent", decimals=1,
    ),
}
