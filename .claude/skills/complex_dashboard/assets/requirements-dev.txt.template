# TODO_DOMAIN dashboard — development extras.
#
# Includes everything in requirements.txt plus test and lint tooling.
# Install on the developer machine, never in production.

-r requirements.txt

pytest>=8.0
dash[testing]>=4.1
ruff>=0.6
mypy>=1.10
