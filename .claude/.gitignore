# Claude Code runtime files — do NOT commit these
agent-activity.log
agent-activity.jsonl
.agent-timers/
.session-pushed
.lead-last-log
.hook-errors.log

# Local settings — may contain machine-specific config or secrets
settings.local.json
